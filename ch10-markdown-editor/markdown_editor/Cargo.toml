[package]
name = "markdown_editor"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { version = "0.7", features = ["web"] }
# ⭐ pulldown-cmark：Rust 的 Markdown 解析库（标准选择）
pulldown-cmark = "0.12"
